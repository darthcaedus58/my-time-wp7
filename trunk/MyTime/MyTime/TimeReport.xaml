<phone:PhoneApplicationPage x:Class="FieldService.TimeReport" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:charting="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=System.Windows.Controls.DataVisualization.Toolkit" xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:FieldService" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:my="clr-namespace:FieldService" xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone" xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone" xmlns:system="clr-namespace:System;assembly=mscorlib" xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit" FontFamily="{StaticResource PhoneFontFamilyNormal}" FontSize="{StaticResource PhoneFontSizeNormal}" Foreground="{StaticResource PhoneForegroundBrush}" Loaded="PhoneApplicationPage_Loaded" Orientation="Portrait" SupportedOrientations="Portrait" mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768" shell:SystemTray.IsVisible="True">
    <phone:PhoneApplicationPage.Resources>
        <!-- Data -->
        <my:ChartData x:Key="ChartData" />
    </phone:PhoneApplicationPage.Resources>
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <!--Pivot Control-->
        <controls:Pivot Title="FIELD SERVICE">
            <!--Pivot item one-->
            <controls:PivotItem Header="report">
                <!--<ScrollViewer Height="Auto" >
                <StackPanel Height="Auto">-->
                <Grid Height="Auto">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid  Row="0">
                            <charting:Chart Style="{StaticResource PhoneChartStyle}" Template="{StaticResource PhoneChartPortraitTemplate}" x:Name="myChart" LegendTitle="legend"  Title="my activity">
                                <charting:Chart.Axes>
                                    <charting:LinearAxis Interval="1" Minimum="0" Orientation="Y" ShowGridLines="True" />
                                </charting:Chart.Axes>
                                <!-- Series -->
                                <charting:LineSeries DependentValuePath="Time" IndependentValuePath="Header" ItemsSource="{StaticResource ChartData}" Title="Time in Hours">
                                    <charting:LineSeries.LegendItemStyle>
                                        <Style TargetType="charting:LegendItem">
                                            <Setter Property="Margin" Value="5 0 5 0" />
                                        </Style>
                                    </charting:LineSeries.LegendItemStyle>
                                </charting:LineSeries>
                            </charting:Chart>
                        </Grid>
                        <Grid Row="1">
                        <ScrollViewer Height="Auto">
                            <ItemsControl ItemsSource="{Binding icReport}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock Style="{StaticResource PhoneTextLargeStyle}" Width="Auto" Text="{Binding Month}" />
                                            <TextBlock Margin="0,15,5,0" Width="Auto" Text="TIME" FontSize="18" FontWeight="Bold" />
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Width="Auto" Text="{Binding Time}" />
                                                <TextBlock Width="Auto" Text=" on " />
                                                <TextBlock Width="Auto" Text="{Binding Days}" />
                                                <TextBlock Width="Auto" Text=" day(s)" />
                                            </StackPanel>
                                            <TextBlock Margin="0,15,5,0" Width="Auto" Text="PLACEMENTS" FontSize="18" FontWeight="Bold" />
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Width="Auto" Text="{Binding Magazines}" />
                                                <TextBlock Width="Auto" Text=" Magazines, " />
                                                <TextBlock Width="Auto" Text="{Binding Books}" />
                                                <TextBlock Width="Auto" Text=" Books, " />
                                                <TextBlock Width="Auto" Text="{Binding Brochures}" />
                                                <TextBlock Width="Auto" Text=" Brochures" />
                                            </StackPanel>
                                            <TextBlock Margin="0,15,5,0" Text="OTHER ACTIVITY" FontSize="18" FontWeight="Bold" />
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Width="Auto" Text="{Binding ReturnVisits}" />
                                                <TextBlock Width="Auto" Text=" Return Visits" />
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Width="Auto" Text="{Binding BibleStudies}" />
                                                <TextBlock Width="Auto" Text=" Bible Studies" />
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
            <!--</StackPanel>-->
        </ScrollViewer>
                    </Grid>
                    </Grid>
            </controls:PivotItem>

            <!--Pivot item two-->
            <controls:PivotItem Header="entries">
                <Grid HorizontalAlignment="Left" VerticalAlignment="Top">
                    <ListBox Height="480" ItemsSource="{Binding lbTimeEntries}" Name="lbEntries" SelectionChanged="lbEntries_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Style="{StaticResource PhoneTextLargeStyle}" Text="{Binding Date}" />
                                    <TextBlock Style="{StaticResource PhoneTextSubtleStyle}" Text="{Binding Hours}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>

    <!--Sample code showing usage of ApplicationBar-->
    <!--<phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton x:Name="appbar_button1" IconUri="/Images/appbar_button1.png" Text="Button 1"/>
            <shell:ApplicationBarIconButton x:Name="appbar_button2" IconUri="/Images/appbar_button2.png" Text="Button 2"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem x:Name="menuItem1" Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem x:Name="menuItem2" Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>-->

</phone:PhoneApplicationPage>